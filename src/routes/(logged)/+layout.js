import { redirect } from "@sveltejs/kit";

export const load = () => {
	/*let datiLocali = localStorage.getItem("jwt");

	if (!datiLocali) {
		redirect(302, "/profilo");
	}

	return { datiLocali };*/
};
