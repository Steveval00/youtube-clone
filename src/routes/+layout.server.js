//chiamate di rete

import { PUBLIC_YOUTUBE_API_KEY } from "$env/static/public";

export const load = (slug, page) => {
	// Load the page data for the given slug
	// and page number

	return { ytId: PUBLIC_YOUTUBE_API_KEY };
};
